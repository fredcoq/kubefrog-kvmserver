apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: superset-dashboard-web-pages
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/fredcoq/kubefrog-kvmserver
    targetRevision: HEAD
    path: superset-dashboard-web-pages
    
      
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true