apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: n8n
  namespace: n8n
spec:
  hosts:
    - n8n.kubefrog.ai
  http:
    - route:
        - destination:
            host: n8n
            port:
              number: 5678
  gateways:
    - apps-network/apps-gateway-cloud
