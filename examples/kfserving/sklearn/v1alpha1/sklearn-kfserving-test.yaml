apiVersion: "serving.kubeflow.org/v1alpha2"
kind: "InferenceService"
metadata:
  name: sklearn-iris
  namespace: kfserving-test
spec:
  default:
    predictor:
      sklearn:
        storageUri: "s3://iris"
