apiVersion: v1
kind: Service
metadata:
  name: imagegen-webui-frontend
  namespace: frederik-coquelet
spec:
  selector:
    app: pytorch-image
  ports:
    - protocol: TCP
      name: http-webui
      port: 8188
      targetPort: 8188
    - protocol: TCP
      name: http-api
      port: 5000
      targetPort: 5000

# authorizations policy
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: imagegen-webui-frontend
  namespace: frederik-coquelet
spec:
  action: ALLOW
  rules:
  - {}
  selector:
    matchLabels:
      app: pytorch-image
  



