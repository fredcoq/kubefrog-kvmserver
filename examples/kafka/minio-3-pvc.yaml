apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio3-pvc
  namespace: default
spec:
    accessModes:
      - ReadWriteMany
    storageClassName: rook-cephfs
    resources:
      requests:
        storage: 10Gi

 
