apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: auth-gateway
  namespace: auth
spec:
  selector:
    istio: ingressgateway
  servers:
  - hosts:
    - auth.82.66.198.32.nip.io
    - auth.192.168.0.100.nip.io
    port:
      name: http
      number: 80
      protocol: HTTP
    # Upgrade HTTP to HTTPS
    tls:
      httpsRedirect: true
  - hosts:
    - auth.82.66.198.32.nip.io
    - auth.192.168.0.100.nip.io
    port:
      name: https
      number: 443
      protocol: HTTPS
    tls:
      mode: SIMPLE
      credentialName: kubeflow-ingressgateway-certs
