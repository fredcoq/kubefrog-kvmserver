
apiVersion: v1
kind: Service
metadata:
  name: postgres-loadbalancer
  namespace: superset
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    cluster-name: superset-postgres-cluster
    spilo-role: master
  
