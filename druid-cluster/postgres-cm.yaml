apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-infrastructure-roles
  namespace: druid-operator
data:
  druid: |
    inrole: [admin, operator]  # following roles will be assigned to the new user
    user_flags:
    - createdb
    - superuser
