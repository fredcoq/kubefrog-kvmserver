apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-druid-hdd-pvc
  namespace: druid-operator
spec:
    storageClassName: rook-ceph-block-hdd
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 120Gi

 
