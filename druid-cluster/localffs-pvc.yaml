apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: localfs-pvc
  namespace: druid-cluster
spec:
    storageClassName: rook-cephfs
    accessModes:
      - ReadWriteMany
    resources:
      requests:
        storage: 10Gi

 
