apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-test-2-pvc
  namespace: dev-velero-2
spec:
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 10Gi

 
