apiVersion: v1
kind: Pod
metadata:
  name: kafka-client
  namespace: kafka
spec:
  containers:
  - name: kafka-client
    image: quay.io/strimzi/kafka:0.37.0-kafka-3.4.0  
    command:
      - sh
      - -c
      - "exec tail -f /dev/null"
